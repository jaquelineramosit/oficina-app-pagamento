{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Local Lambda Runner",
            "type": "debugpy",
            "request": "launch",
            "module": "resources_local.local_runner",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "console": "integratedTerminal",
            "justMyCode": false
        }
    ]
}